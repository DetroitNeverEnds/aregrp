const Config = {
    // Cколько объектовы отображать на главной странице в каждом из разделов
    pageSizeMain: 6,
    // Сколько объектовы отображать в каталоге в каждом из разделов
    pageSizeCatalogue: 12,

    // maps
    map: {
        // Минимальное расстояние от границы карты до объектов (Измеряется в градусах)
        minBound: 0.01,
        /**
         * Какой процент от расстояния между дальними объектами брать до границ карты
         * Например, если расстояние между дальними объектами 100 км,
         * то границы карты будут рассчитываться как 100 км * 0.1 = 10 км
         */
        boundRatio: 0.3,
    },

    referral: {
        /** Query-параметр реферального кода в URL */
        searchParam: 'ref',
        /** Имя cookie для передачи кода на бэкенд при оплате (совпадает с REFERRAL_CODE_COOKIE_NAME) */
        cookieName: 'referral_code',
        /** Срок жизни cookie реферала, дней */
        cookieDays: 7,
        /** Паттерн валидного UUID реферального кода */
        codeUuidPattern:
            '^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$',
        cookiePath: '/',
        cookieSameSite: 'lax' as const,
    },
};

export default Config;
